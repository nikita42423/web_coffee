# Задания для команды разработки

## Общая архитектура проекта

**Технологии:** Python, Django, Django Templates, SQLite, Bootstrap 5
**Структура:** Модульная с приложениями в папке `apps/`
**Дополнительное приложение:** `reviews` – единая система отзывов, рейтингов и избранного для всех типов контента

## Приложение: Books (Книги)

### Статус: ✅ ГОТОВО

1. **Модели (готово):**
   - Модель `Book` с полями: название, автор, описание, текст книги (content), поля доступности (субтитры, жестовый перевод, аудиоописание), теги, рейтинг
   - Связь с общей системой отзывов через GenericRelation

2. **Админ-панель (готово):**
   - Настроено отображение модели Book в админке
   - Добавлены фильтры по доступности
   - Добавлен поиск по названию и автору

3. **Views (готово):**
   - `book_list()` – список всех книг с пагинацией
   - `book_detail()` – детальная страница книги с оценками, комментариями и избранным
   - `book_search()` – поиск книг по различным критериям (название, автор, теги) с фильтрами по доступности
   - `book_by_accessibility()` – фильтрация по типам доступности

4. **Шаблоны (готово, адаптированы под Bootstrap 5):**
   - `books/list.html` – сетка карточек книг с фильтрами
   - `books/detail.html` – детальная страница
   - `books/partials/book_card.html` – компонент карточки книги

5. **Функциональность (реализована через приложение `reviews`):**
   - Оценка книг (рейтинг 1‑5)
   - Комментарии к книгам
   - «Избранное» для авторизованных пользователей

## Приложение: Films (Фильмы)

### Статус: 🟡 ЧАСТИЧНО ГОТОВО (требует доработки)

1. **Модели (готово):**
   - Модель `Film` с полями: название, режиссер, год, описание, продолжительность, поля доступности, теги, рейтинг, ссылка на видео, платформа
   - Связь с общей системой отзывов через GenericRelation

2. **Админ-панель (отсутствует):**
   - Не настроено отображение модели Film в админке (нет admin.py)
   - Требуется добавить валидацию года выпуска
   - Требуется добавить предпросмотр ссылок на видео

3. **Views (отсутствуют):**
   - `film_list()` – не реализован
   - `film_detail()` – не реализован
   - `film_by_year()` – не реализован
   - `film_with_subtitles()` – не реализован

4. **Шаблоны (отсутствуют):**
   - `films/list.html` – нет
   - `films/detail.html` – нет
   - `films/partials/film_card.html` – нет

5. **Функциональность (не реализована):**
   - Интеграция с YouTube/Vimeo (iframe)
   - Система отметок «Просмотрено»
   - Таймкоды для важных моментов фильма

## Приложение: Education (Образование)

### Статус: ✅ ГОТОВО

1. **Модели (готово):**
   - Модель `Course` с полями: название, преподаватель, описание, продолжительность, уровень сложности, поля доступности (включая текстовую расшифровку), теги, рейтинг, ссылка на курс, платформа
   - Связь с общей системой отзывов через GenericRelation

2. **Админ-панель (готово):**
   - Настроено отображение модели Course в админке
   - Добавлен выбор уровня сложности через dropdown
   - Добавлена валидация ссылок на курсы

3. **Views (готово):**
   - `course_list()` – список курсов с фильтрацией по уровню и доступности
   - `course_detail()` – детальная страница курса с оценками, комментариями и избранным
   - `course_by_level()` – курсы по уровню сложности (реализован, но отсутствует шаблон `education/level.html`)

4. **Шаблоны (готово):**
   - `education/list.html` – список курсов (готово)
   - `education/detail.html` – детальная страница с программой курса (готово)
   - `education/partials/course_card.html` – компонент карточки курса (готово)

5. **Функциональность (реализована через приложение `reviews`):**
   - Оценка курсов (рейтинг 1‑5)
   - Комментарии к курсам
   - «Избранное» для авторизованных пользователей

## Приложение: Forum (Форум)

### Статус: ✅ ГОТОВО (базовый функционал)

1. **Модели (готово):**
   - `ForumCategory` – категории форума (MPTT-иерархия)
   - `ForumTopic` – темы обсуждения
   - `ForumPost` – сообщения в темах (вложенность через parent)
   - `ForumReply` – не отдельная модель, ответы реализованы через поле `parent` модели `ForumPost`

2. **Админ-панель (готово):**
   - Настроена иерархия категорий
   - Возможность модерации сообщений
   - Статистика активности пользователей

3. **Views (готово):**
   - `forum_index()` – список категорий
   - `category_topics()` – темы в категории
   - `topic_detail()` – обсуждение темы
   - `create_topic()` – создание новой темы
   - `create_post()` – добавление сообщения
   - `edit_post()` – редактирование сообщения
   - `forum_search()` – поиск по форуму

4. **Шаблоны (готово, адаптированы под Bootstrap 5):**
   - `forum/index.html` – главная страница форума
   - `forum/category.html` – страница категории
   - `forum/topic.html` – страница обсуждения
   - `forum/create_topic.html` – форма создания темы
   - `forum/edit_post.html` – форма редактирования сообщения
   - `forum/search.html` – результаты поиска
   - `forum/base_forum.html` – базовый шаблон форума

5. **Функциональность (базовая реализована, расширенная отсутствует):**
   - Создание тем и сообщений (готово)
   - Вложенные ответы (готово)
   - Поиск по форуму (готово)
   - WYSIWYG редактор для сообщений – нет
   - Уведомления об ответах – нет
   - Система лайков/дизлайков – нет
   - Возможность прикреплять файлы – нет

## Приложение: Reviews (Общие отзывы)

### Статус: ✅ ГОТОВО (новое, не описано в оригинальном задании)

1. **Модели (готово):**
   - `Review` – единый отзыв для книг, фильмов, курсов с полями: пользователь, оценка (0–5), комментарий, поля доступности (субтитры, жестовый перевод, аудиоописание, текстовая расшифровка)
   - `Favorite` – избранное для любого типа контента

2. **Админ-панель (готово):**
   - Отображение отзывов и избранного

3. **Services (готово):**
   - `get_average_rating()` – средний рейтинг для объекта
   - `get_review_count()` – количество отзывов
   - `get_reviews_for()` – список отзывов
   - `add_review()` – добавление отзыва
   - `toggle_favorite()` – добавление/удаление из избранного
   - `is_favorited()` – проверка, находится ли объект в избранном

4. **Интеграция:**
   - Используется во всех приложениях (Books, Films, Education) через GenericRelation

## Основное приложение: Main

### Статус: 🟡 ЧАСТИЧНО ГОТОВО

1. **Главная страница (готово):**
   - `index.html` обновлён для использования Bootstrap 5
   - Добавлены секции «Популярное», «Новинки», «Рекомендации»
   - Поиск интегрирован (готово)

2. **Поиск (готово):**
   - `search_results.html` адаптирован под Bootstrap
   - Реализована сортировка результатов
   - Добавлены расширенные фильтры поиска

3. **Навигация (частично готово):**
   - Меню категорий присутствует
   - Хлебные крошки не реализованы
   - Адаптивное меню реализовано через Bootstrap

## Общие задачи для всех приложений

### Инфраструктура:
1. **Шаблоны:**
   - Наследование от `base.html` – готово
   - Использование Bootstrap 5 компонентов – готово
   - Адаптивный дизайн для мобильных устройств – готово

2. **Стили:**
   - Минимальные кастомные стили в `<style>` тегах – частично
   - Использование Bootstrap утилит – готово
   - Темная/светлая тема – отсутствует

3. **Доступность:**
   - ARIA-атрибуты для всех интерактивных элементов – частично
   - Поддержка клавиатурной навигации – частично
   - Контрастные цвета для текста – готово (Bootstrap по умолчанию)

4. **Тестирование:**
   - Написать unit-тесты для моделей – не выполнено
   - Тестирование views – не выполнено
   - Интеграционные тесты для основных сценариев – не выполнено

### Сроки:
- **Финальная сборка:** до 14:00 22 февраля

### Критерии качества:
1. **Код:** Чистый, с комментариями, PEP8 – частично соблюдается
2. **Архитектура:** Модульная, слабая связность – соблюдается (добавлено приложение `reviews`)
3. **UI/UX:** Интуитивный интерфейс, Bootstrap 5 – соблюдается
4. **Доступность:** Поддержка специальных возможностей – частично
5. **Документация:** README с инструкцией по запуску – есть, требует обновления

## Распределение по команде:

**Frontend‑разработчики:**
- Адаптация шаблонов под Bootstrap 5 – выполнена для Books, Forum, Main, частично для Education
- Верстка карточек контента – выполнена
- Навигация и пользовательский интерфейс – выполнена

**Backend‑разработчики:**
- Реализация views и логики – выполнена для Books, Education, Forum; отсутствует для Films
- Настройка админ‑панели – выполнена для Books, Education, Forum; отсутствует для Films
- Интеграция поиска и фильтрации – выполнена

**Fullstack‑разработчики:**
- Создание моделей и миграций – выполнено
- Написание тестов – не выполнено
- Документация и деплой – в процессе

**Дизайнер/UX:**
- Прототипы интерфейса – выполнены
- Цветовая схема и типографика – используются Bootstrap по умолчанию
- Тестирование доступности – не выполнено

---

*Документ обновлён 22.02.2026 по результатам аудита кодовой базы.*
*Следующие шаги: завершить приложение Films, добавить недостающие шаблоны для Education, реализовать расширенную функциональность форума, написать тесты.*
